# Frontend (Next.js)

初始化:
```bash
cd frontend && npx create-next-app@latest .
```
建议组件(见 docs/DEVELOPMENT.md §3):
MotifGraph(React Flow)、WaveformPlayer(Wavesurfer.js)、UploadForm、MotifCard、
RemixPanel、ComparePlayer、Loading。`lib/api.ts` 只调后端,不直连 ACE-Step。
